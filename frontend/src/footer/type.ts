//Main(login) props

//Navbar content props
type ProjectList = {
  link: string;
  projectName: string;
};

export type ProjectListProps = {
  projects: ProjectList[];
};

export type creditsProps = {
  credits: string[];
};
